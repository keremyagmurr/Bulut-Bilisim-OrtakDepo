version: '3.8'

services:
  trivia-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped

# Redis entegrasyonu için (isteğe bağlı):
# redis:
#   image: redis:7-alpine
#   ports:
#     - "6379:6379"
#   restart: unless-stopped
